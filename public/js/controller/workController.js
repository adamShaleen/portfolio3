angular.module('adamShaleen').controller('workController', function($scope, service) {

    // Carousel of project icons
    $(document).ready(function() {
        $('.carousel_icons_mobile').slick({
            // insert something!
        });
    });

});  // closing tag
